.container.mt-5.px-1.px-sm-0
  .row
    .col-md-3.offset-md-1.mb-4
      h3.bg-light.text-center
        | 注文履歴一覧
  .row
    table.table.table-bordered
      tr.table-secondary
        th[style="width: 15%"]
          | 注文日
        th
          | 配送先
        th[style="width: 15%"]
          | 注文商品
        th[style="width: 8%"]
          | 支払金額
        th[style="width: 10%"]
          | ステータス
        th[style="width: 11%"]
          | 注文詳細
      - @orders.each do |order|
        tr
          td
            = order.created_at.strftime('%Y/%m/%d')
          td
            |  〒
            = order.postcode
            = order.address
            = order.name
          td.align-middle
            - order.order_items.each do |order_item|
              = order_item.item.name
          td.align-middle
            = order.total_price.to_s(:delimited)
            | 円
          td.align-middle
            = order.status
          td.align-middle
            = link_to "表示する", order_path(order), class: "btn btn-primary"
